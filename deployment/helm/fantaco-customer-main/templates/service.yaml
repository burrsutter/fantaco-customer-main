apiVersion: v1
kind: Service
metadata:
  name: {{ include "fantaco-customer-main.fullname" . }}
  labels:
    {{- include "fantaco-customer-main.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "fantaco-customer-main.selectorLabels" . | nindent 4 }}
