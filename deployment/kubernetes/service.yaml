apiVersion: v1
kind: Service
metadata:
  name: fantaco-customer-service
  labels:
    app: fantaco-customer-main
spec:
  type: ClusterIP
  selector:
    app: fantaco-customer-main
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
